# ─────────────────────────────────────────────────────────────
#  COPY THIS FILE TO .env  (never commit .env to git!)
# ─────────────────────────────────────────────────────────────

# MongoDB Atlas
# Where to get it: https://cloud.mongodb.com
#   → Your Cluster → Connect → Drivers → copy the string
#   → Replace <password> with your actual password
#   → Add /portfolio before the ?
MONGODB_URI=mongodb+srv://devportfolio:wehuh93ji3h@cluster0.hwkzffo.mongodb.net/portfolio?retryWrites=true&w=majority

# JWT Secret — make this long and random, never share it
# Generate one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace-with-a-long-random-string-at-least-32-chars

# Cloudinary — Image hosting (free tier: 25GB)
# Where to get it: https://cloudinary.com → Dashboard
CLOUDINARY_CLOUD_NAME=djfmqcudt
CLOUDINARY_API_KEY=213381383951134
CLOUDINARY_API_SECRET=vy6E6nMLSBKz6_B3wXVjOvVbAGw

# Port (Render sets this automatically — don't change)
PORT=4000

# Frontend URL for CORS (set this to your Vercel URL in production)
# Example: https://my-portfolio.vercel.app
# During local dev: http://localhost:5173
FRONTEND_URL=http://localhost:5173
